#!/bin/sh

# shellcheck source=./setup
## TODO: What happens if $0 have no slashes?  Can that happen?
. "${0%/*}/setup"

# Test that EMACS_BIN is set to emacs if neither EMACS or INSIDE_EMACS
# is set

env -S -i - \
	PATH="${0%/*}:$defaultpath"  \
	"$buttercup" select emacs |
	checkline "$testdir/emacs -batch $defaultargs select emacs"
